# EE6483 Project 2 — 【实验负责人任务计划】
> 本文档供**实验负责人**使用，目标是跑完所有实验并产出结果交给组员写报告。  
> 截止日期: 2026年4月17日（周五）23:59

---

## 项目目录结构

```
project/
├── data/
│   ├── train/
│   │   ├── dog/          # 10000张（全部使用）
│   │   └── cat/          # 10000张（全部使用）
│   ├── val/
│   │   ├── dog/          # 2500张（全部使用）
│   │   └── cat/          # 2500张（全部使用）
│   └── test/             # 500张（无标签，id=1~500）
├── src/
│   ├── dataset.py
│   ├── model.py
│   ├── train.py
│   ├── predict.py
│   └── utils.py
├── experiments/          # 存放各实验的日志/结果
│   ├── exp1_simplecnn/
│   ├── exp2_resnet50_scratch/
│   ├── exp3_resnet50_pretrained/
│   ├── exp4_lr_ablation/
│   ├── exp5_augmentation_ablation/
│   └── exp6_cifar10/
├── checkpoints/
├── outputs/              # 最终交付给组员的结果图表
├── sampleSubmission.csv  # 老师提供的格式模板（id: 1~500, label用0占位）
├── submission.csv        # 你最终生成的预测结果
└── requirements.txt
```

---

## TASK 1 — 环境搭建

**文件**: `requirements.txt`

```
torch>=2.0.0
torchvision>=0.15.0
numpy
pandas
matplotlib
seaborn
scikit-learn
pillow
tqdm
```

```bash
pip install -r requirements.txt
```

---

## TASK 2 — 数据加载与预处理

**文件**: `src/dataset.py`

### 数据量

A800 服务器资源充足，**全部使用**：
- 训练集: dog 10000 + cat 10000 = **20000张**
- 验证集: dog 2500 + cat 2500 = **5000张**
- 测试集: **500张**（id 1~500，与 sampleSubmission.csv 对应）

### 测试集注意事项

sampleSubmission.csv 格式已确认：
```
id,label
1,0
2,0
...
500,0
```
- `id` 为图片编号（1~500），对应测试集图片文件名
- `label` 是占位符（全0），你需要用模型预测后替换
- 最终提交的 submission.csv 保持完全相同的格式，只替换 label 列

### Dataset 类（`src/dataset.py`）

```python
import os
from PIL import Image
from torch.utils.data import Dataset

class DogCatDataset(Dataset):
    """用于 train/val 有标签集"""
    def __init__(self, root_dir, transform=None):
        self.samples = []
        self.transform = transform
        for label, cls in enumerate(['cat', 'dog']):   # cat=0, dog=1
            cls_dir = os.path.join(root_dir, cls)
            for fname in os.listdir(cls_dir):
                if fname.lower().endswith(('.jpg', '.jpeg', '.png')):
                    self.samples.append((os.path.join(cls_dir, fname), label))

    def __len__(self):
        return len(self.samples)

    def __getitem__(self, idx):
        path, label = self.samples[idx]
        img = Image.open(path).convert('RGB')
        if self.transform:
            img = self.transform(img)
        return img, label


class DogCatTestDataset(Dataset):
    """用于 test 无标签集，返回图片和 id"""
    def __init__(self, test_dir, transform=None):
        self.transform = transform
        self.samples = []
        for fname in sorted(os.listdir(test_dir),
                            key=lambda x: int(os.path.splitext(x)[0])):
            if fname.lower().endswith(('.jpg', '.jpeg', '.png')):
                img_id = int(os.path.splitext(fname)[0])
                self.samples.append((os.path.join(test_dir, fname), img_id))

    def __len__(self):
        return len(self.samples)

    def __getitem__(self, idx):
        path, img_id = self.samples[idx]
        img = Image.open(path).convert('RGB')
        if self.transform:
            img = self.transform(img)
        return img, img_id
```

### Transform 定义

```python
from torchvision import transforms

train_transform = transforms.Compose([
    transforms.Resize((256, 256)),
    transforms.RandomCrop(224),
    transforms.RandomHorizontalFlip(p=0.5),
    transforms.RandomRotation(degrees=15),
    transforms.ColorJitter(brightness=0.2, contrast=0.2, saturation=0.2),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406],
                         std=[0.229, 0.224, 0.225]),
])

val_transform = transforms.Compose([
    transforms.Resize((224, 224)),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406],
                         std=[0.229, 0.224, 0.225]),
])
```

---

## TASK 3 — 模型定义

**文件**: `src/model.py`

需要实现以下三个模型（用于对比实验）：

### Model A: SimpleCNN（从零训练的基础对比模型）

```python
import torch.nn as nn

class SimpleCNN(nn.Module):
    def __init__(self):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(3, 32, 3, padding=1), nn.ReLU(), nn.BatchNorm2d(32), nn.MaxPool2d(2),
            nn.Conv2d(32, 64, 3, padding=1), nn.ReLU(), nn.BatchNorm2d(64), nn.MaxPool2d(2),
            nn.Conv2d(64, 128, 3, padding=1), nn.ReLU(), nn.BatchNorm2d(128), nn.MaxPool2d(2),
            nn.Conv2d(128, 256, 3, padding=1), nn.ReLU(), nn.BatchNorm2d(256), nn.MaxPool2d(2),
        )
        self.classifier = nn.Sequential(
            nn.AdaptiveAvgPool2d(1),
            nn.Flatten(),
            nn.Dropout(0.5),
            nn.Linear(256, 2)
        )

    def forward(self, x):
        return self.classifier(self.features(x))
```

### Model B: ResNet-50（从零训练，无预训练权重）

```python
import torchvision.models as models

def get_resnet50_scratch(num_classes=2):
    model = models.resnet50(weights=None)
    model.fc = nn.Sequential(
        nn.Dropout(0.5),
        nn.Linear(model.fc.in_features, 2)
    )
    return model
```

### Model C: ResNet-50（ImageNet预训练 + 微调，主力模型）

```python
def get_resnet50_pretrained(num_classes=2):
    model = models.resnet50(weights='IMAGENET1K_V1')
    # 替换分类头
    in_features = model.fc.in_features
    model.fc = nn.Sequential(
        nn.Dropout(0.5),
        nn.Linear(in_features, 256),
        nn.ReLU(),
        nn.Dropout(0.3),
        nn.Linear(256, 2)
    )
    return model
```

---

## TASK 4 — 训练脚本

**文件**: `src/train.py`

### 固定随机种子（必须在最顶部调用）

```python
import random, numpy as np, torch

def set_seed(seed=42):
    random.seed(seed)
    np.random.seed(seed)
    torch.manual_seed(seed)
    torch.cuda.manual_seed_all(seed)
    torch.backends.cudnn.deterministic = True
    torch.backends.cudnn.benchmark = False
```

### 标准训练配置

```python
config = {
    'lr': 1e-4,
    'weight_decay': 1e-4,
    'epochs': 20,
    'batch_size': 64,      # A800可以用更大的batch
    'num_workers': 8,
    'seed': 42,
}
```

### 训练循环

```python
def train_one_epoch(model, loader, optimizer, criterion, device):
    model.train()
    total_loss, correct, total = 0, 0, 0
    for imgs, labels in tqdm(loader, leave=False):
        imgs, labels = imgs.to(device), labels.to(device)
        optimizer.zero_grad()
        outputs = model(imgs)
        loss = criterion(outputs, labels)
        loss.backward()
        optimizer.step()
        total_loss += loss.item() * len(labels)
        correct += (outputs.argmax(1) == labels).sum().item()
        total += len(labels)
    return total_loss / total, correct / total


@torch.no_grad()
def evaluate(model, loader, criterion, device):
    model.eval()
    total_loss, correct, total = 0, 0, 0
    for imgs, labels in loader:
        imgs, labels = imgs.to(device), labels.to(device)
        outputs = model(imgs)
        loss = criterion(outputs, labels)
        total_loss += loss.item() * len(labels)
        correct += (outputs.argmax(1) == labels).sum().item()
        total += len(labels)
    return total_loss / total, correct / total


def train(model, train_loader, val_loader, config, save_path, device):
    criterion = nn.CrossEntropyLoss()
    optimizer = torch.optim.Adam(model.parameters(),
                                  lr=config['lr'],
                                  weight_decay=config['weight_decay'])
    scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(
        optimizer, T_max=config['epochs'])

    best_val_acc = 0
    patience_counter = 0
    history = {'train_loss': [], 'train_acc': [], 'val_loss': [], 'val_acc': []}

    for epoch in range(config['epochs']):
        tr_loss, tr_acc = train_one_epoch(model, train_loader, optimizer, criterion, device)
        val_loss, val_acc = evaluate(model, val_loader, criterion, device)
        scheduler.step()

        history['train_loss'].append(tr_loss)
        history['train_acc'].append(tr_acc)
        history['val_loss'].append(val_loss)
        history['val_acc'].append(val_acc)

        print(f"Epoch [{epoch+1}/{config['epochs']}] "
              f"Train Loss={tr_loss:.4f} Acc={tr_acc:.4f} | "
              f"Val Loss={val_loss:.4f} Acc={val_acc:.4f}")

        if val_acc > best_val_acc:
            best_val_acc = val_acc
            torch.save(model.state_dict(), save_path)
            patience_counter = 0
        else:
            patience_counter += 1
            if patience_counter >= 5:
                print("Early stopping triggered.")
                break

    return history, best_val_acc
```

---

## TASK 5 — 预测与生成 submission.csv

**文件**: `src/predict.py`

```python
import pandas as pd
import torch

@torch.no_grad()
def predict_and_save(model, test_loader, device, save_path='submission.csv'):
    model.eval()
    all_ids, all_preds = [], []

    for imgs, img_ids in test_loader:
        imgs = imgs.to(device)
        outputs = model(imgs)
        preds = outputs.argmax(dim=1).cpu().tolist()
        all_ids.extend(img_ids.tolist())
        all_preds.extend(preds)

    df = pd.DataFrame({'id': all_ids, 'label': all_preds})
    df = df.sort_values('id').reset_index(drop=True)
    df.to_csv(save_path, index=False)
    print(f"Saved {len(df)} predictions to {save_path}")
    print(df['label'].value_counts().rename({1: 'dog', 0: 'cat'}))
```

格式与 sampleSubmission.csv 完全一致：`id`（1~500）, `label`（1=dog, 0=cat）

---

## TASK 6 — 需要跑的全部实验清单

以下每个实验跑完后，把指定的**产出文件**保存到 `outputs/` 文件夹，交给组员。

---

### 实验 EXP-A: SimpleCNN（基础对比）
**目的**: 作为 baseline，说明"从零训练的简单CNN"效果有限

```bash
python src/train.py --model simplecnn --epochs 30 --lr 1e-3 --batch_size 64 \
    --save_path checkpoints/simplecnn.pth \
    --log_dir experiments/exp1_simplecnn/
```

**需要保存的产出**:
- [ ] `outputs/fig_simplecnn_training_curve.png` — 训练/验证 loss & acc 曲线（两张图）
- [ ] `outputs/table_simplecnn_result.txt` — 最终验证集 accuracy（一个数字）

---

### 实验 EXP-B: ResNet-50（从零训练，无预训练权重）
**目的**: 对比"有无预训练"的影响

```bash
python src/train.py --model resnet50_scratch --epochs 30 --lr 1e-3 --batch_size 64 \
    --save_path checkpoints/resnet50_scratch.pth \
    --log_dir experiments/exp2_resnet50_scratch/
```

**需要保存的产出**:
- [ ] `outputs/fig_resnet50scratch_training_curve.png`
- [ ] `outputs/table_resnet50scratch_result.txt`

---

### 实验 EXP-C: ResNet-50（预训练 + 微调）——主力模型
**目的**: 主要模型，用于生成最终 submission.csv

```bash
python src/train.py --model resnet50_pretrained --epochs 20 --lr 1e-4 --batch_size 64 \
    --save_path checkpoints/resnet50_pretrained_best.pth \
    --log_dir experiments/exp3_resnet50_pretrained/
```

**需要保存的产出**:
- [ ] `outputs/fig_main_training_curve.png` — 主模型训练曲线（必须给组员）
- [ ] `outputs/fig_confusion_matrix.png` — 验证集混淆矩阵
- [ ] `outputs/fig_correct_samples.png` — 5张正确分类样本（含标注）
- [ ] `outputs/fig_wrong_samples.png` — 5张错误分类样本（含标注）
- [ ] `outputs/table_classification_report.txt` — precision/recall/F1（sklearn输出）
- [ ] `submission.csv` — 测试集预测结果（替换 sampleSubmission.csv 的 label 列）

---

### 实验 EXP-D: 超参数消融——学习率对比
**目的**: 说明学习率选择的依据

只改 lr，其他与 EXP-C 相同：

| 实验 | lr |
|------|----|
| EXP-D1 | 1e-3 |
| EXP-D2 | 1e-4 ✓（主力） |
| EXP-D3 | 5e-5 |

**需要保存的产出**:
- [ ] `outputs/table_lr_ablation.csv` — 三行，每行: lr, best_val_acc

格式:
```
lr,val_acc
1e-3,0.9350
1e-4,0.9721
5e-5,0.9580
```

---

### 实验 EXP-E: 数据增强消融对比
**目的**: 说明数据增强的有效性

| 实验 | 描述 |
|------|------|
| EXP-E1 | 无任何增强（只 Resize + Normalize） |
| EXP-E2 | 只加 Flip |
| EXP-E3 | 完整增强（Flip + Rotation + ColorJitter）✓ |

模型固定用 ResNet-50 预训练，lr=1e-4

**需要保存的产出**:
- [ ] `outputs/table_augmentation_ablation.csv`

格式:
```
augmentation,val_acc
none,0.9410
flip_only,0.9612
full,0.9721
```

---

### 实验 EXP-F: 模型汇总对比表
整合 EXP-A ~ EXP-C 结果，手动填写以下表格并保存：

- [ ] `outputs/table_model_comparison.csv`

格式:
```
model,pretrained,val_acc,params_M,train_time_min
SimpleCNN,No,?,0.3,?
ResNet50-Scratch,No,?,25.6,?
ResNet50-Pretrained,Yes,?,25.6,?
```

---

### 实验 EXP-G: CIFAR-10 多分类

**数据集**: 自动下载，10类，每类6000训练 / 1000测试

```bash
python src/train_cifar10.py --epochs 30 --lr 1e-4 --batch_size 128 \
    --save_path checkpoints/cifar10_best.pth \
    --log_dir experiments/exp6_cifar10/
```

注意：CIFAR-10 图像 32×32，输入 ResNet 前需 resize 到 224×224（或用更小分辨率+小模型）

**需要保存的产出**:
- [ ] `outputs/fig_cifar10_training_curve.png`
- [ ] `outputs/fig_cifar10_confusion_matrix.png` — 10×10混淆矩阵
- [ ] `outputs/table_cifar10_per_class_accuracy.csv` — 每类的准确率
- [ ] `outputs/table_cifar10_overall_accuracy.txt` — 总体测试集准确率

---

### 实验 EXP-H: CIFAR-10 类别不平衡处理

构造不平衡数据集（例如：让5个类只保留10%的训练数据），然后对比：

| 实验 | 方法 |
|------|------|
| EXP-H0 | 不做任何处理（baseline） |
| EXP-H1 | WeightedRandomSampler（加权采样） |
| EXP-H2 | Class-Weighted CrossEntropyLoss（加权损失） |

```python
# 构造不平衡数据集（在 train_cifar10.py 中实现）
# 将类别 0,1,2,3,4 的训练数据随机保留 10%
# 类别 5,6,7,8,9 保留全部

# 方法一：WeightedRandomSampler
from torch.utils.data import WeightedRandomSampler
class_counts = [count_per_class...]  # 统计各类数量
weights_per_class = 1.0 / torch.tensor(class_counts, dtype=torch.float)
sample_weights = weights_per_class[targets]
sampler = WeightedRandomSampler(sample_weights, len(sample_weights), replacement=True)

# 方法二：加权损失
class_weights = total_samples / (num_classes * class_counts)
criterion = nn.CrossEntropyLoss(weight=torch.tensor(class_weights).to(device))
```

**需要保存的产出**:
- [ ] `outputs/table_imbalance_comparison.csv`

格式:
```
method,overall_acc,minority_class_avg_acc
no_handling,?,?
weighted_sampler,?,?
weighted_loss,?,?
```

---

## 产出汇总清单（交给组员的全部文件）

训练完成后，`outputs/` 文件夹里应该包含以下内容，打包发给写报告的组员：

```
outputs/
├── fig_simplecnn_training_curve.png          → 报告 问题f（模型对比）
├── fig_resnet50scratch_training_curve.png    → 报告 问题f
├── fig_main_training_curve.png               → 报告 问题c（主模型描述）
├── fig_confusion_matrix.png                  → 报告 问题d（分类结果）
├── fig_correct_samples.png                   → 报告 问题e（样本分析）
├── fig_wrong_samples.png                     → 报告 问题e
├── fig_cifar10_training_curve.png            → 报告 问题g
├── fig_cifar10_confusion_matrix.png          → 报告 问题g
├── table_classification_report.txt           → 报告 问题d
├── table_simplecnn_result.txt                → 报告 问题f
├── table_resnet50scratch_result.txt          → 报告 问题f
├── table_model_comparison.csv                → 报告 问题f（汇总表）
├── table_lr_ablation.csv                     → 报告 问题d（超参选择）
├── table_augmentation_ablation.csv           → 报告 问题f（数据处理对比）
├── table_cifar10_per_class_accuracy.csv      → 报告 问题g
├── table_cifar10_overall_accuracy.txt        → 报告 问题g
└── table_imbalance_comparison.csv            → 报告 问题h
```

以及根目录的：
```
submission.csv                                → 直接提交给助教
```

---

## 运行顺序建议

```bash
# 第1步：主力模型（优先跑，用于生成submission）
python src/train.py --model resnet50_pretrained ...

# 第2步：生成 submission.csv
python src/predict.py --model_path checkpoints/resnet50_pretrained_best.pth

# 第3步：对比实验（SimpleCNN、ResNet-scratch）
python src/train.py --model simplecnn ...
python src/train.py --model resnet50_scratch ...

# 第4步：消融实验（lr对比、augmentation对比）
# 跑3次 train.py，改 --lr 参数

# 第5步：CIFAR-10 标准训练
python src/train_cifar10.py ...

# 第6步：CIFAR-10 不平衡实验
python src/train_cifar10.py --imbalanced --method weighted_sampler ...
python src/train_cifar10.py --imbalanced --method weighted_loss ...
```
